package com.plefy.app.model

/**
 * The direction in which a column should be sorted.
 */
enum class SortDirection {
    /** Ascending order (smallest first). */
    ASC,

    /** Descending order (largest first). */
    DESC
}
